server:
  port: 6067
spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/passengerdb'
    username: root
    password: smit
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
eureka:
  client:
    service-url:
      defaultZone: 'http://localhost:7779/eureka/'
